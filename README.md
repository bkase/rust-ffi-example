# Rust v0.4 FFI Example

## Instructions

Treat `run` in `runtime.c` as `main` for a C program. The rust function will be passed in a parameter to `run`.

## Build

```
./libmake.sh
rustc hello.rs
```

## Acknowledgements

Based off of https://gist.github.com/2159727 from @crabtw

